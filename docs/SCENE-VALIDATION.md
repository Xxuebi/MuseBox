# 1.1.9 场景功能验收记录

验证日期：2026-09-03。

## 已验证

- Release 构建保持 0 警告、0 错误，全量 138/138 自动化测试通过。
- `.mubo` 描述升级为版本 2，保存独立组合实体、图层名称和最多 32 层的嵌套关系；版本 1 平面组合继续迁移读取。
- 图层校验拒绝重复 ID、缺失父级、循环、超深嵌套和空组合；层级写入与场景替换保持事务完整性。
- 单文件 .iscene 往返：静态图片、重复资源去重、GIF 原始帧、倍速及暂停帧、文字格式、链接、笔迹、图片组、位置尺寸旋转层级、背景透明度、视图及可再次编辑的封面。
- 原资料库离线后，在独立空资料库仅凭场景文件打开、重新映射对象 ID、继续编辑和再次保存。
- 首次保存、另存不改原文件、修改标记、替换前保存/取消、不保存退出、取消退出、取消文件选择、只读目标及写入失败保护。
- 外部文件哈希冲突、取消写入、资源缺失/篡改、重复或恶意 ZIP 路径、伪造超限长度、未知版本、不安全富文本均拒绝。
- 导入中途注入 SQLite 失败，验证事务回滚后旧画板和绑定完整保留。
- 保存前提交当前 GIF 控制状态与尚未结束的文字编辑；窗口尺寸适配屏幕。
- 启动初始化门控和文件激活控制器、独立进程通过命名管道转交含中文/空格的路径、重复打开不覆盖未保存编辑。
- 图片与注释失效文件链接实际点击：圆角提示、进入原对象链接设置、清空、重新设置；切换选择不误改其他对象，网页地址保持不变。
- 缺失字体提示且保留原字体名称；菜单、三选项提示截图检查；旧菜单动画和画板编辑回归。
- 本轮未调用 Inno Setup，也未生成或覆盖安装包；安装脚本只同步 1.1.9 版本元数据。

## 未执行的实机验收

没有运行安装程序、替换正在运行的版本或改写用户实际文件关联。安装后的资源管理器双击流程仍需在安装后验收；当前覆盖启动参数/初始化控制器和跨进程通信，不等同于真实 Explorer 双击测试。

没有在另一台实体电脑执行；跨电脑可移植性通过“源资料库离线 + 独立空资料库”验证。

## 发布

- 便携版：publish/v1.1.9/MuseBox.exe，需保留同目录依赖文件。
- 本版本不生成安装包；旧版本目录与用户资料不替换。
- 安装器使用内置图标规避本机 Windows 资源更新错误 110，应用和文件关联仍使用原应用图标。
- 场景为手动保存；本机恢复数据不等同于已更新 .iscene 文件。

## 复现命令

    dotnet run --project MuseBox.Tests/MuseBox.Tests.csproj -c Release
    dotnet run --project MuseBox.Tests/MuseBox.Tests.csproj -c Release -- 场景
    dotnet publish MuseBox.csproj -c Release -p:PublishProfile=win-x64
    .tools/InnoSetup/ISCC.exe Installer/MuseBox.iss
