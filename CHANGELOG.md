# 更新日志

本文件记录 MuseBox 的主要版本变化。项目介绍、使用方法与构建说明请查看 [README.md](README.md)。


## 1.2.0

- 汇总 1.1.27–1.1.36 尚未公开发布的更新：左侧素材区、独立画板开关、链接／复制导入、按抽屉记忆导入方式、自动保存和紧凑设置界面。
- 包含 GIF 保存与复制修复、素材拖动预览、当前抽屉打开 .mubo、未保存内容保护及同文件保存后重载修复。
- 恢复传统 EXE 安装版，与便携 ZIP 一同发布；本次不发布 MSIX，也不需要导入测试证书。
- 重写项目介绍和两种版本的下载、升级与数据路径说明，移除 README 中的界面预览图。

## 1.1.36

- 抽屉菜单在“打开画板”下新增“打开 .mubo 文件”，将所选文件载入当前抽屉；“导入图像”移至“导出所有图像”上方。
- 替换未保存的抽屉内容（包含素材区）或已绑定场景的未保存修改前，提供“保存／取消／覆盖”；取消、无效文件或保存失败不替换原内容。
- 修复选择同一 .mubo 文件并先保存时载入旧快照的问题；保留文件拖入小窗时独立打开画板的行为。

## 1.1.35

- 导入图像确认框更新说明，新增“保存选择，下次不提醒”；按抽屉记忆导入方式，仅成功导入后保存选择，取消或失败不关闭提醒。
- 画板设置新增“导入图片方式提醒”复选框，可重新开启当前抽屉的询问；移除小窗设置中的外部链接保存方式区域。
- 移除应用语言下方说明；进入保存和加载页不再自动聚焦或选中路径文本，仍可手动编辑。
- 自动保存默认开启，保留已有显式关闭设置及原有保存间隔。
- 修复静态图片残留 GIF 播放状态、旧资源格式标记错误及外链替换造成的“GIF 状态无效”保存失败；有效动画播放状态保留，场景读取安全校验不变。

## 1.1.34

- 素材区开关改为每个画板独立保存，随视口和场景往返；新画板及旧版缺少该字段时默认开启。
- 小窗收集、截图和抽屉导入按目标画板开关分流；关闭仅迁移当前画板素材，事务失败恢复，其他抽屉不受影响，迁移仍可撤回。
- 素材右键选中指向的图片，右键已选素材保留多选范围。
- 常规设置改为紧凑圆角分组行，移除“小窗默认置顶”；图钉仍可切换小窗置顶。
- 自动保存、保存间隔及撤回上限移入“保存和加载”，画板保存路径置于最上方；功能说明使用悬停提示。

## 1.1.33

- 修复素材区主体与侧边收纳按钮的接缝，二者共用位移动画；顶部工具栏新增素材区按钮。
- 素材区全局开关迁入画板设置，保留关闭时的批量移入、确认及失败恢复。
- 常规设置的功能说明改为悬停提示，移除常驻说明文字并保持标题与开关居中对齐。
- 画板复制 GIF 保留原始动图字节，提供 GIF、文件及 HTML 剪贴板格式，不再优先转换为静态 PNG；普通图片复制保持原行为。

## 1.1.32

- 素材区改为左侧工具条式收纳，小三角控制滑入与展开，保持原有宽高调整和画板视图。
- 移除顶部标题、数量文字和图片文件名，保留悬停信息及 GIF 标记；图片边缘增加淡阴影。
- 点击素材空白区域清除素材选择，不产生整行或空白区域的选中效果。
- 拖动素材时显示跟随鼠标的图片预览，多选显示叠放与数量，释放或取消后自动清理。
- 素材区右键菜单新增“全部移入画板”，无需先全选，沿用事务化移动与撤回。

## 1.1.31

- 新增默认开启的左侧素材区：圆角覆盖面板、展开收纳、宽高调整、虚拟化缩略图列表及独立的 Ctrl / Shift 多选。
- 小窗截图、抽屉导入和拖入抽屉的图片先进入素材区；直接拖入或粘贴到画板保持原行为。素材按抽屉持久保存。
- 拖入画板或“移入画板”将素材事务化移到根级前景，按 18 单位间距整理；删除、移入、批量迁移支持按 ID 撤回／重做，不覆盖新收到的素材。
- 小窗设置可关闭素材区：确认后将所有待整理素材移到各自画板右侧，失败恢复数据和设置；尚未打开的画板在本次运行中接入迁移历史。
- 场景格式升级为 v4，兼容 v1–v3；素材参与场景保存、外链刷新及复制转换、自动保存与全部图像导出，不参与画板图层、适应、排列和合成导出。
- 素材缺失链接保留占位和路径；删除素材或抽屉不删除外部原图。关闭素材区时打开含素材的场景会自动整理到画板，并标记未保存。

## 1.1.30

- 抽屉菜单新增“导入图像”；多选导入和拖入具体抽屉时，可选择链接原文件或复制进画板，并整批验证。
- 外部图片按绝对路径独立引用，原图修改、删除和恢复会自动刷新；缺失图片保留占位和路径，删除抽屉不会删除原图。
- 保存外部图片可选择保持链接或转换为内部副本，支持记住选择、设置页恢复询问，以及自动保存沿用处理方式。
- 场景格式升级为 v3，继续读取 v1/v2；保存使用固定资源快照、暂存及事务回滚，复制转换仅影响当前画板并支持撤回。
- 明暗主题的圆角确认窗口、缺失资源导出报错，以及链接导入、保存、刷新、撤回和失败回滚回归测试。

## 1.1.29

- 限制设置导航的手型光标与悬停效果只作用于左侧按钮，右侧静态内容恢复正常光标。
- 关于页新增功能简介、开发者、许可证、运行环境和本地数据说明，并提供 GitHub 主页、下载与反馈入口。

## 1.1.28

- 设置左侧选中项改为完整圆角的浅色底块，移除侧边强调线。
- 恢复导航图标、文字及右侧设置内容的正常主题文字颜色，修正选中区域右侧裁切。

## 1.1.27

- 在小窗设置中新增自动保存开关与 1–120 分钟的保存间隔，默认关闭、默认间隔 5 分钟。
- 自动保存仅更新已有 `.mubo` 绑定且有修改的画板，不为临时画板弹出对话框或自动创建文件；外部文件冲突时安全跳过。
- 重制设置窗口左侧导航为统一的圆角选中卡片和强调色标记，补齐悬停反馈，并移除默认虚线焦点框。
## 1.1.26

- 移除画板右键子菜单关闭时的淡出动画，保留打开动画，避免子菜单收起时闪烁。
- 重新整理画板右键菜单：常用编辑操作置顶，组合、排列和层级集中排列，创建、保存与画板功能分区展示。
- 将四项层级操作收进“层级”子菜单，缩短一级菜单并保持常用命令易于访问。
- 新增可配置的复制、保存、另存为快捷键，默认分别为 Ctrl+C、Ctrl+S、Ctrl+Shift+S；低频功能继续保持未分配。

## 1.1.25

- 网格改为类似 PureRef 的自适应细分：放大画板时逐级显示更小的网格，缩小时自动合并，且世界坐标原点保持稳定。
- 默认网格间距更密，主线调整为每五小格一条；线宽、点大小和透明度进一步降低。
- 元素移动吸附跟随当前可见网格间距，确保视觉网格与实际吸附位置一致。
- 版本相关测试改为读取程序集版本，后续生成新便携版时不再依赖写死的旧版本号。

## 1.1.24

- 将网格、重置相机、重置相机缩放、适应全部和去色统一收进画板右键菜单的“画板”子菜单。
- “吸附到网格”切换后保持右键菜单展开，便于连续调整网格显示和吸附状态。
- 线网格与点网格改为随画板缩放和平移的世界坐标样式，并使用半透明主次线与半透明点提升明暗背景下的辨识度。
- 修正测试程序对旧可执行文件名的依赖，使独立便携构建也能完整验证场景单实例转交。

## 1.1.23

- 将逐张原格式导出升级为“导出图像”窗口，可选择保存位置、原始／PNG／JPG／BMP 格式和可编辑命名模板。
- 命名模板使用简短的通用百分号符号（如 `%n`、`%02i`、`%s`），支持名称、序号与补零、场景、日期时间、类型、百分号转义和实时预览；旧长字段模板仍可读取并自动转为短写法。
- 导出窗口改为可拖动的紧凑圆角卡片，输入框、下拉列表、按钮和明暗主题视觉与应用其他对话框保持一致。
- 批量导出按图层前后顺序编号，并在写入前统一检查非法名称、未知字段、批次重名和目标冲突。
- 新增覆盖全部、跳过冲突和取消流程；覆盖导出使用暂存与旧文件备份，失败时完整恢复。
- 格式转换保持资源原始像素尺寸，PNG 保留透明通道，JPG 使用质量 92，JPG/BMP 的透明区域填充白色，GIF 使用当前保存帧。
- 成功导出后全局记忆路径、格式和模板；合成 PNG、场景内容、撤回历史及未保存状态保持不变。

## 1.1.22

- 画板右键菜单新增“保存”，统一提供保存、另存为，以及按全部或所选范围导出。
- 导出支持逐张保留图片原格式，或将图片、文字、绘制和符合范围的组合背景合成为透明 2 倍 PNG。
- 小窗抽屉菜单将“打开”改为直接“打开画板”，并新增“导出所有图像”。
- 支持把 `.mubo` 和旧版 `.iscene` 场景拖入小窗独立打开；混合拖入抽屉时仍会把普通图片导入该抽屉。

## 1.1.21

- 修复通过画板任务栏按钮退出特殊模式失效的问题，并在托盘一级菜单增加清晰可见的“退出画板模式”入口。
- 放大画板模式进入和退出提示窗，提升标题、说明文字及暗色主题下的可读性。
- 修复透明模式下点击画板空白处不能取消元素选择的问题。
- 智能置顶改为监听目标窗口前台切换并立即同步层级，减少切回目标窗口时画板短暂落到后方的闪烁。

## 1.1.20

- 画板右键菜单新增“无视鼠标”“画板透明”和“智能置顶”三种互斥模式。
- 新增可配置的系统级退出快捷键、任务栏退出和托盘退出入口，并完整恢复进入模式前的画板状态。
- 智能置顶支持点击选择外部窗口、目标隐藏时同步隐藏以及目标关闭后安全退出。

## 1.1.19

- 左、右、上、下对齐增加等距排列子菜单，保留原来的普通对齐。
- 水平／垂直分布改为固定 18 单位间距的单行／单列排列，并在另一方向居中，两个元素即可使用。
- 新增排列规则与嵌套菜单回归覆盖，继续支持混合元素、组合、选区、撤回和事务保存。

## 1.1.18

- 画板右键菜单新增“排列”子菜单，统一支持自动排列、四向对齐及水平／垂直等距分布。
- 排列支持图片、文字、绘制和嵌套组合；没有选择时处理全画板，有选择时只处理选中范围。
- 混合元素位置通过同一事务保存，失败不改变位置或撤回历史；排列可撤回／重做。

## 1.1.17

- 画板背景透明度条、百分比与影响图片开关使用同一行垂直居中布局。
- 画板设置窗口随紧凑内容自动缩小，移除底部多余空白，并保留操作按钮上方间距。

## 1.1.16

- 将主窗口、画板、编辑器、设置、对话框和覆盖层源码归档到清晰的 `Views` 子目录。
- 将应用级功能、场景验证文档和安装脚本分别整理到对应目录，并统一安装脚本名称与版本。
- 将移动后的 WPF 图标引用改为程序集资源 URI，保持窗口与测试运行时资源加载正常。

## 1.1.15

- 修复画板外观设置中文字与开关未对齐的问题。
- 修复右键菜单关闭时的闪烁。

## 1.1.14

- 精简画板背景色块与布局，并改进色块边缘、悬停和选中反馈。
- 跟随系统外观时显示当前实际生效的明暗模式。

## 1.1.13

- 重排画板背景颜色、透明度和图片透明度同步选项。
- 透明度滑杆支持点按与连续拖动。

## 1.1.12

- 图层面板支持双击组合居中。
- 改进调色板透明度滑杆、色块对比度和深色主题图标。
- 移除控件上不必要的虚线焦点边框。

## 1.1.11

- 修复嵌套组合移动与逐层双击选择逻辑。
- 图片、文字、绘制和组合均可从图层面板双击居中。

## 1.1.10

- 图层面板实时响应组合变化，并支持调整面板宽度与独立滚动。
- 完善嵌套组合选择、多选、背景命中和图层右键菜单。
- 重命名编辑器增加确定与取消操作。

## 1.1.9

- 改进图层多选、单击展开和父子元素选择。
- 双击图层元素时保持当前画板缩放并居中。
- 修复嵌套组合背景遮挡和剪贴板图片默认名称。

## 1.1.8

- 新增可调整宽度的图层面板，以及图片、文字、绘制和组合的层级树。
- 支持图层选择同步、重命名、拖动排序和跨组合移动。
- 新增嵌套组合、层级持久化、撤回/重做与 `.mubo` 版本 2。

## 1.1.7

- 绘制内容和文字可加入组合。
- 边框颜色与边框参数改为分体式工具按钮，粗细和外扩宽度支持独立设置。

## 1.1.6

- 重新设计组合背景与边框工具栏色条。
- 新增边框粗细、背景框外扩宽度和双击往返聚焦。

## 1.1.5

- 新增组合背景、整体选择、锁定和自动收纳。
- 支持拖入、拖出组合时的范围提示与实时边界更新。

## 1.1.4

- 修复深色主题下部分文字、输入框和对话框未跟随外观的问题。
- 修复开关初始化动画以及小窗置顶按钮状态。

## 1.1.3

- 修复自绘标题栏按钮命中、悬停反馈和画板首次显示黑框。

## 1.1.2

- 修复标题栏点击、双击最大化和窗口外圈黑边。

## 1.1.1

- 改进圆角窗口、自绘标题栏与画板窗口边界。

## 1.1.0

- 新增完整浅色、深色与跟随系统主题。
- 更新主窗口、小窗和画板的自绘标题栏。

## 1.0.42

- 新增全局明暗主题、语言入口和开关动画。

## 1.0.41

- 更新应用主图标和沉浸收集图标。

## 1.0.40

- 新增场景缩略图与文件集成管理。

## 1.0.39

- 更新极光风格应用图标与场景文件图标。

## 1.0.38

- 完成 MuseBox 品牌更新、沉浸菜单和 `.mubo` 场景扩展名。

## 1.0.37

- 改进素材收集流程与沉浸窗口高度计算。

## 1.0.36

- 新增沉浸收集模式与抽屉动画。

## 1.0.35

- 修复小窗口显示与交互问题。

## 1.0.34

- 新增可移植场景文件。

## 1.0.31–1.0.33

- 完善早期画板、素材管理与稳定性功能。
